apiVersion: v1
kind: Service
metadata:
  name: {{ include "ocp-python-api.fullname" . }}
  labels:
    {{- include "ocp-python-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ocp-python-api.selectorLabels" . | nindent 4 }}
